# Dependencies for experiments_new/ (rebuttal experiments). Python 3.11 or 3.12.
# Install with the setup script (setup_env.ps1 / setup_env.sh) -- yahpo-gym must be
# installed with --no-deps because its metadata pins ConfigSpace<=0.6.1 and numpy<2,
# which do not build on current Python; ConfigSpace 1.2 works with the shim in common/yahpo.py.
numpy>=2.0,<3
scipy>=1.13
pandas>=2.2
matplotlib>=3.8
scikit-learn>=1.4
scikit-posthocs>=0.9
joblib>=1.3
tqdm>=4.66
pyyaml>=6.0
optuna>=4.0
cmaes>=0.11
torch>=2.2            # Optuna GPSampler (GP-BO baseline); CPU build is enough
ConfigSpace>=1.2,<2
onnxruntime>=1.17
# yahpo-gym==1.0.2  -> installed separately with --no-deps (see setup_env.*)
# smac>=2.1          -> optional, Linux/WSL only (SMAC baseline)
